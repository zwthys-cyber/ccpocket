name: ccpocket
description: "Mobile client for Codex and Claude — chat, approve tools, and manage sessions from your phone."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.131.1+249

environment:
  sdk: ^3.13.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  web_socket_channel: ^3.0.3
  webview_flutter: ^4.14.1
  marionette_flutter: ^0.6.0
  shared_preferences: ^2.5.0
  sqflite: ^2.4.2
  sqflite_common_ffi: ^2.4.0+3
  flutter_local_notifications: ^20.0.0
  flutter_markdown: ^0.7.7+1
  markdown: ^7.3.0
  google_fonts: ^8.0.0
  bonsoir: ^6.0.1
  mobile_scanner: ^7.1.4
  app_links: ^7.0.0
  speech_to_text: ^7.3.0
  http: ^1.6.0
  crypto: ^3.0.6
  flutter_bloc: ^9.1.0
  provider: ^6.1.5
  flutter_hooks: ^0.21.3+1
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  share_plus: ^12.0.1
  image_picker: ^1.1.2
  path: ^1.9.1
  path_provider: ^2.1.5
  url_launcher: ^6.3.1
  dartssh2: ^2.10.0
  flutter_secure_storage: ^10.0.0
  uuid: ^4.5.1
  super_clipboard: ^0.9.1
  firebase_core: ^4.4.0
  firebase_messaging: ^16.1.1
  shorebird_code_push: ^2.0.5
  purchases_flutter: ^9.13.1
  package_info_plus: ^9.0.0
  auto_route: ^11.1.0
  collection: ^1.19.1
  extended_image: ^10.0.1
  flutter_svg: ^2.0.17
  smooth_page_indicator: ^2.0.1
  talker: ^5.1.13
  talker_flutter: ^5.1.13
  talker_bloc_logger: ^5.1.13
  scroll_to_index: ^3.0.1
  skeletonizer: ^2.1.3
  expandable_page_view: ^1.2.1
  highlight: ^0.7.0
  syntax_highlight: ^0.5.0
  in_app_review: ^2.0.11
  flutter_slidable: ^4.0.3
  super_drag_and_drop: ^0.9.1
  media_kit: ^1.2.6
  media_kit_video: ^2.0.1
  media_kit_libs_video: ^1.0.7
  file_selector: ^1.1.0
  flutter_painter: ^2.1.0

dependency_overrides:
  # Temporary: use 16KB page-aligned fork until irondash/irondash#77 is merged
  irondash_engine_context:
    git:
      url: https://github.com/Sabbar-Engineering/irondash
      path: engine_context/dart

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  altive_lints: ^4.0.0
  # Dart 3.13 forbids `final` on regular parameters; Freezed 4 generates
  # compatible constructors (rrousselGit/freezed#1352).
  freezed: ^4.0.0-dev.3
  flutter_launcher_icons: ^0.13.1
  build_runner: ^2.5.4
  bloc_test: ^10.0.0
  patrol_finders: ^3.1.0
  url_launcher_platform_interface: ^2.3.2
  json_serializable: ^6.9.5
  auto_route_generator: ^10.4.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  config:
    # speech_to_text's SwiftPM manifest still targets macOS 10.14, while the
    # plugin uses APIs that require 10.15+. CocoaPods respects the app's 12.0
    # deployment target, so keep SPM off for this project for now.
    enable-swift-package-manager: false
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/icon.png
    - assets/icon_light_outline.png
    - assets/icon_pro_copper_emerald.png
    - assets/docs/
    - assets/store/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
  fonts:
    - family: JetBrainsMono
      fonts:
        - asset: assets/fonts/code/JetBrainsMono-Regular.ttf
    - family: IoskeleyMono
      fonts:
        - asset: assets/fonts/code/IoskeleyMono-Regular.ttf
    - family: DejaVuSansMono
      fonts:
        - asset: assets/fonts/code/DejaVuSansMono.ttf
